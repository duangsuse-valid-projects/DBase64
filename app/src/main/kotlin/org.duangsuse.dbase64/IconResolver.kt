package org.duangsuse.dbase64

import android.R

object IconResolver {
  var androidRDrawable = android.R.drawable::class.java

  fun hasIcon(name: String): Boolean {
  }
  fun icon(name: String): Int {}
}
